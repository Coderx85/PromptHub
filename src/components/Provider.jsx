'use client';

// next-auth SessionProvider replaced with better-auth
// better-auth handles session management automatically

const Provider = ({ children }) => (
  <>
    {children}
  </>
)

export default Provider;